<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kwak.mapper.BoardMapper">
	<insert id="signUp">
		insert into member(name, id, pw, nickname) values (#{name}, #{id}, #{pw}, #{nickname})
	</insert>
	
	<select id="login" resultType="int">
		select count(id) from member where id = #{id}
	</select>
	
	<select id="list" resultType="edu.kwak.domain.PostVO">
		select * from post order by pno desc
	</select>
	
	<select id="read" resultType="edu.kwak.domain.PostVO">
		select title, nickname, postdate, content from post where pno = #{pno}
	</select>
	
	<insert id="insert">
		insert into post(title, nickname, content) values( #{title}, #{nickname}, #{content})
		<selectKey keyProperty = "pno" order="AFTER" resultType="long">
			select last_insert_id()
		</selectKey>
	</insert>
	
	<update id="update">
		update post set title = #{title}, content = #{content} where pno = #{pno}
	</update>
	
	<delete id="deletePost">
		delete from post where pno = #{pno}
	</delete>
</mapper>